import { PageLoadingState } from "@/components/ui/loading-state";

export default function Loading() {
  return <PageLoadingState title="Loading events" detail="Preparing the event workspace..." />;
}
